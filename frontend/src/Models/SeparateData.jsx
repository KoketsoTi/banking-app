const client = (data) => {
    console.log(data);
    return   console.log(data);
}

const Account = () => {
    
}

const Loans = () => {
    
}

const Card = () => {
    
}

const functions ={
    client,
    Account,
    Loans,
    Card,
}

export default functions;